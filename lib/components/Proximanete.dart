import 'package:flutter/material.dart';


class Proximante extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: EdgeInsets.symmetric(vertical:532.0),
      child:Column(
        children: [
          Text("Coming soon",style: TextStyle(color: Colors.white,fontSize: 26.0),),
        ],
      ),

    );

  }

}